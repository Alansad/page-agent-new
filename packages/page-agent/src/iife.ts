/**
 * IIFE entry - exposes `PageAgent` as the global `PageAgent` class.
 *
 * No side effects: does NOT auto-initialize or show the panel.
 */
import { PageAgent } from './PageAgent'

export default PageAgent

